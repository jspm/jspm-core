import url from 'browserify-url';
export default url;
export var Url = url.Url;
export var format = url.format;
export var resolve = url.resolve;
export var resolveObject = url.resolveObject;
export var URL = typeof URL !== 'undefined' ? URL : null;
